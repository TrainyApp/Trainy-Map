server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Pretty URLs: /@username and /userId serve index.html, JS reads pathname
  location / {
    try_files $uri $uri/ /index.html;
  }
}
